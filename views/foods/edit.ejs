<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit a Food</title>
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>
        <h1>Edit a Food in the Pantry</h1>
        <form action="/users/<%= user._id %>/foods/<%= food._id %>?_method=PUT" method="POST">

            <label for="name">Name:</label>
            <input type="text" id="name" name="name" required value="<%= food.name %>" />

            <label for="color">Color:</label>
            <input type="text" id="color" name="color" required value="<%= food.color %>" />

            <button type="submit">Submit</button>
        </form>


</body>

</html>